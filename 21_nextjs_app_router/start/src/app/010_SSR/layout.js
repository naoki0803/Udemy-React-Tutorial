export default function SubLayout({ children }) {
    return (
        <div
            style={{
                background: "#f0f3bd",
                color: "#05668d",
                padding: "15px",
            }}
        >
            {children}
        </div>
    );
}
